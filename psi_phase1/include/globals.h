#include <stdint.h>
#include <stdbool.h>

#ifndef GLOBALS
#define GLOBALS

extern bool terminate; //global lag for terminating program

extern int64_t new_sym_inc; // global auto-incrementer for new-symbol

#endif